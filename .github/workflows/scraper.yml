name: Run Scraper

on:
  schedule:
    - cron: "0 * * * *"   # every hour
  workflow_dispatch:       # allow manual trigger

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Chrome & Chromedriver
        run: |
          sudo apt-get update
          sudo apt-get install -y chromium-browser chromium-chromedriver
          which chromium-browser
          which chromedriver

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper
<<<<<<< HEAD
        run: |
          set -e
          echo "Starting SportPesa scraper..."
          python src/data_fetch/sportpesa.py || echo "⚠️ Scraper exited with error, continuing gracefully"
=======
        run: | 
          python src/main.py
>>>>>>> 86140a024c743484cff82f7b451e943b742c703d
